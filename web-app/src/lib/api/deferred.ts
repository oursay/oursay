/** Explicit user-facing copy for surfaces deferred past W5. */

export const DEFERRED_EDIT_HISTORY = "Edit history is not available yet.";

export const DEFERRED_PASSKEY_RECOVERY =
  "Passkey recovery is not available yet — use email OTP on a new device.";

export const DEFERRED_LEGAL = (label: string) => `${label} is not available yet.`;

export const DEFERRED_JURISDICTIONS_SETTINGS =
  "Use the jurisdiction selector in the header to add or remove jurisdictions.";

export const DEFERRED_CLAIM_PROFILE =
  "Profile claiming is not available yet — verify as the office holder to take over this page.";
