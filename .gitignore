# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
.output/
.vercel/
.netlify/

# Astro
.astro/

# Environment / secrets — never commit
.env
.env.*
!.env.example
!.env.test.example
!.env.prod.example
*.key.json
*.key
*.pem
*credentials*.json

# Logs
*.log
npm-debug.log*

# immudb-test — local container data (bind mount) and generated artifacts
immudb-test/.immudb-data/
immudb-test/.state/
immudb-test/out/

# public-record — anchor output (anchors.jsonl + per-block bundle files)
public-record/.anchors/
public-record/out/

# @oursay/evm-anchor — Hardhat build artifacts + local deploy address
# (also covered by evm-anchor/.gitignore)
evm-anchor/artifacts/
evm-anchor/.evm/
evm-anchor/cache/
evm-anchor/coverage/
evm-anchor/types/
# Ignore local Ignition deploys (e.g. Hardhat 31337); keep Sepolia (11155111) for reproducibility.
evm-anchor/ignition/deployments/*
!evm-anchor/ignition/deployments/chain-11155111/
!evm-anchor/ignition/deployments/chain-11155111/**

# @oursay/identity — simulated dev passkey custody (DevPasskeyConnector). NEVER commit key material.
.oursay-dev/

# @oursay/api — live Postmark smoke-test artifact (delete to re-run api/test/32-postmark-otp.spec.ts)
api/test/.output/

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/
.ignore/
.claude/
.cursor/

# Local agent prompts and session notes (not for the repo)
.agents/
.screenshots/