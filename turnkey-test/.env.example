# Parent Turnkey organization (loads from repo root .env when present)
TURNKEY_ORG_ID=
TURNKEY_API_PUB=
TURNKEY_API_PRIV=

# Optional: path to Turnkey API key JSON (publicKey + privateKey fields)
# TURNKEY_API_KEY_FILE=../turnkey-api-credentials-*.key.json
